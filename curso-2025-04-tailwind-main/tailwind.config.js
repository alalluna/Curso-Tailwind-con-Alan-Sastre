/** @type {import('tailwindcss').Config} En tailwind 4 ya se hace directamente desde CSS*/
module.exports = {
    content: ["./**/*.{html,js}"],
    theme: {
      extend: {},
    },
    plugins: [],
}